# Leave SUPABASE_* empty to use the local JSON file (JOBS_FILE, default ../data/jobs.json,
# falling back to ../data/sample_jobs.json so the UI can be tried with no setup).
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
JOBS_FILE=

# Password for the page. Empty = no login (local use only).
APP_PASSWORD=
